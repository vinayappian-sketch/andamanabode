// This component is no longer used as images are now static.
// It is kept to avoid breaking imports in a larger app, but can be safely removed.
import React from 'react';

const Spinner: React.FC = () => {
  return null;
};

export default Spinner;